server:
  port: 8888

spring:
  security:
    user:
      name: root
      password: s3cr3t
  cloud:
    config:
      server:
        git:
          uri: https://github.com/aKimtsPro/config-repo
          default-label: master
          clone-on-start: true
